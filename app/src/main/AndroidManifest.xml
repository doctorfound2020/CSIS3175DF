<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoctorViewMessage"></activity>
        <activity android:name=".DoctorViewHistory" />
        <activity android:name=".DoctorResponseMessage" />
        <activity android:name=".DoctorReminder" />
        <activity android:name=".DoctorCheckAppointment" />
        <activity android:name=".PatientViewMessage" />
        <activity android:name=".PatientViewHistory" />
        <activity android:name=".PatientSendMessage" />
        <activity android:name=".PatientReminder" />
        <activity android:name=".PatientOnlineHelp" />
        <activity android:name=".PatientCheckAppointment" />
        <activity android:name=".PatientBookAppointment" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>